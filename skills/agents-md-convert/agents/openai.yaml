interface:
  display_name: agents-md-convert
  short_description: >-
    Convert a repo to the AGENTS.md-canonical convention (AGENTS.md holds the
    instructions; CLAUDE.md becomes a one-line @AGENTS.md import), or check an
    existing conversion. Converts existing instructions; does not author them
    from scratch.
policy:
  allow_implicit_invocation: false
