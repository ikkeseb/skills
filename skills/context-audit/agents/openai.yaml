interface:
  display_name: "Context Audit"
  short_description: "Audit a project's CLAUDE.md / Claude Code context architecture"
  default_prompt: "Use $context-audit to audit this repository's CLAUDE.md and Claude Code context architecture. Analysis-only; the subject is Claude Code context, whatever harness runs the audit."
policy:
  allow_implicit_invocation: false
