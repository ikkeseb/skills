interface:
  display_name: "Draw.io Diagram"
  short_description: "Generate a native .drawio XML diagram"
  default_prompt: "Use $drawio to produce a .drawio diagram file for the topic at hand."
policy:
  allow_implicit_invocation: false
