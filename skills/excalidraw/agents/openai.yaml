interface:
  display_name: "Excalidraw"
  short_description: "Build and visually verify an .excalidraw diagram"
  default_prompt: "Use $excalidraw to build, validate, render, inspect, and deliver an editable .excalidraw diagram for the topic at hand."
policy:
  allow_implicit_invocation: false
